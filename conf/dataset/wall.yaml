# Original wall_single dataset.
_target_: "datasets.wall_dset.load_wall_slice_train_val"
n_rollout: null
normalize_action: ${normalize_action}
data_path: /nas/ada/dev/dino/data/wall_single
split_ratio: 0.9
split_mode: "random"
transform:
  _target_: "datasets.img_transforms.default_transform"
  img_size: ${img_size}
# Which position array to read.
states_file: states_true.pth
